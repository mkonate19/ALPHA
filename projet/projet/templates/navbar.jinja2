<!-- navbar.jinja2 -->
<style>
    nav {
        background-color: #333;
        overflow: hidden;
    }

    nav ul {
        list-style-type: none;
        margin: 0;
        padding: 0;
        overflow: hidden;
    }

    nav li {
        float: left;
    }

    nav li a {
        display: block;
        color: white;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
    }

    nav li a:hover {
        background-color: #ddd;
        color: black;
    }
</style>

<!-- navbar.jinja2 -->
<nav>
    <ul>
        <li><a href="{{ request.route_url('new_home') }}">Home</a></li>
        <li><a href="{{ request.route_url('chat') }}">Chat</a></li>
        
        {% if 'user_authenticated' in request.session %}
            <li><a href="{{ request.route_url('logout') }}">Déconnecter</a></li>
        {% else %}
            <li><a href="{{ request.route_url('login') }}">Connexion</a></li>
        {% endif %}
    </ul>
</nav>

